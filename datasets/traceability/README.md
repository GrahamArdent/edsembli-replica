---
id: doc.datasets.traceability.readme
type: document
title: Traceability Matrix
version: 0.1.0
status: draft
tags: [traceability, datasets, coverage]
refs: []
updated: 2026-01-11
---

# Traceability Matrix

This folder contains traceability datasets that connect:
- **Frames** → **Indicators** → **Evidence Patterns** → **Comment Templates** → **References**

## Purpose
The traceability matrix enables:
1. Coverage analysis (do all indicators have templates?)
2. Audit trails (which reference supports this template?)
3. Gap identification (which scenarios need more patterns?)

## Files

| File | Format | Description |
|------|--------|-------------|
| `matrix.csv` | CSV | Human-readable traceability export |
| `matrix.parquet` | Parquet | Machine-optimized format for analytics |

## Generation

The matrix is generated by `scripts/generate_matrix.py` (planned).

```bash
python scripts/generate_matrix.py
```

## Schema

| Column | Type | Description |
|--------|------|-------------|
| `frame_id` | string | e.g., `frame.belonging` |
| `indicator_id` | string | e.g., `indicator.belonging.relationships` |
| `evidence_pattern_id` | string | e.g., `evidence.pattern.block_play` |
| `template_id` | string | e.g., `template.comment.belonging.key_learning.01` |
| `ref_ids` | array | Supporting references |
| `section` | string | `key_learning`, `growth`, or `next_steps` |

## Contract (invariants)

- A row represents a *trace link* from one template to one indicator, optionally supported by an evidence pattern and one or more references.
- `frame_id`, `indicator_id`, `template_id`, and `section` are required for every row.
- `evidence_pattern_id` is optional but recommended when the template implies an observation archetype.
- `ref_ids` may be empty, but when present must contain only `ref.*` IDs.

Recommended uniqueness key:

- (`template_id`, `indicator_id`, `section`, `evidence_pattern_id`)

Recommended denormalized fields (optional):

- `frame_title`, `indicator_title`, `template_title` for human-readable exports.

## Expected joins

- `frame_id` and `indicator_id` must exist in `taxonomy/`.
- `template_id` must exist in `templates/comment_templates.yaml`.
- `evidence_pattern_id` (if present) must exist in `evidence/`.
- Every `ref_id` in `ref_ids` must exist in `references/bibliography.yaml`.

## Status
*Matrix generation script not yet implemented. See ROADMAP.md Phase 3.*
