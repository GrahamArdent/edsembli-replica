# Source dependency list (unpinned)
# Compile to a pinned lock with:
#   pip-compile requirements.in --output-file requirements.lock.txt

# --- Core schema + parsing ---
pydantic>=2.6
ruamel.yaml>=0.18
markdown-it-py>=3.0
jinja2>=3.1

# --- Data + traceability matrix ---
pandas>=2.1
pyarrow>=14.0
duckdb>=0.10

# --- Validation + testing ---
jsonschema>=4.21
pytest>=8.0
ruff>=0.1.0

# --- Readability analysis ---
textstat>=0.7

# --- Documentation ---
mkdocs-material>=9.5.0
mkdocs-macros-plugin>=1.0.0

# --- CLI ---
typer>=0.9.0
rich>=13.0.0

# --- Dev workflow (optional but recommended) ---
pre-commit>=3.6

# --- Supply chain / safety ---
detect-secrets>=1.5

# --- Dependency locking ---
pip-tools>=7.4

# --- Type checking ---
pyright>=1.1
